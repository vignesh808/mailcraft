#!/bin/sh
# Auto-generated by SnakeFlow. Triggers Quality Hub via agent-loop IPC.
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if command -v node >/dev/null 2>&1; then
  node -e 'const fs=require("fs"),p=require("path");const d=process.argv[1];fs.mkdirSync(d,{recursive:true});fs.writeFileSync(p.join(d,"trigger-run.json"),JSON.stringify({kind:"quality",scope:"workspace",ts:Date.now()},null,2))' "$SCRIPT_DIR"
else
  TS=$(date +%s%3N 2>/dev/null || echo $(($(date +%s) * 1000)))
  mkdir -p "$SCRIPT_DIR"
  printf '%s\n' "{\"kind\":\"quality\",\"scope\":\"workspace\",\"ts\":${TS}}" > "$SCRIPT_DIR/trigger-run.json"
fi
